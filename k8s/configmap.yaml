apiVersion: v1
kind: ConfigMap
metadata:
  name: names-app-config
  namespace: names-app
data:
  # Application Configuration
  MAX_NAME_LENGTH: "50"
  SERVER_HOST: "0.0.0.0"
  SERVER_PORT: "8000"
  LOG_LEVEL: "INFO"
  DB_ECHO: "false"
  
  # Database Connection Parameters (non-sensitive)
  DB_HOST: "db-service"
  DB_PORT: "5432"
