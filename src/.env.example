# Names Manager - Environment Configuration Example
# Copy this file to .env and modify values as needed

# Database Configuration
# Database credentials
POSTGRES_USER=names_user
POSTGRES_PASSWORD=names_pass
POSTGRES_DB=namesdb

# The full database connection URL (constructed from above variables)
DB_URL=postgresql+psycopg2://names_user:names_pass@db:5432/namesdb

# Application Configuration
# Maximum length for name field (default: 50)
MAX_NAME_LENGTH=50

# Server Configuration
# Host address to bind the server (default: 0.0.0.0 for all interfaces)
SERVER_HOST=0.0.0.0

# Port number for the server (default: 8000)
SERVER_PORT=8000

# Frontend Configuration
# External port for the frontend service
FRONTEND_PORT=8080

# Logging Configuration
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO

# Database Debugging
# Enable SQLAlchemy query logging (default: false)
# Set to "true" to enable detailed SQL query logging
DB_ECHO=false

# Example configurations for different environments:

# Development environment:
# LOG_LEVEL=DEBUG
# DB_ECHO=true
# SERVER_HOST=127.0.0.1
# FRONTEND_PORT=3000

# Production environment:
# LOG_LEVEL=WARNING
# DB_ECHO=false
# MAX_NAME_LENGTH=100
# POSTGRES_PASSWORD=your_secure_password_here

# Testing environment:
# DB_URL=sqlite:///:memory:
# LOG_LEVEL=ERROR